# Agent Instructions

Add repository-specific rules for coding, testing, style, and workflow here.

## Coding Rules
- (example) Prefer explicit names; avoid abbreviations.
- (example) Keep all code in-memory; no external dependencies.
- (example) Document all public methods.

## Testing Rules
- (example) Include unit tests for every new public method.
- (example) Keep tests deterministic; no sleeps.

## Workflow
- (example) Use `go test ./...` and `go test ./... -race` before finishing.
